#include <string>
#include <iostream>
#include "shuffle.cpp"
#include "card.h"
#include "command.h"
#include "player.h"
using namespace std;

int main(int argc, char** argv[]){
    
    // Determines whether player or Computer
    for(int i < 1; i < 5; i++){
        cout << "Is player " << i << " a human(h) or a computer(c)?" << endl;
        if
    }
}